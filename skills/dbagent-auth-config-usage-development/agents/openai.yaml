interface:
  display_name: "DBAgent 认证配置用量开发"
  short_description: "实现登录、配置、密钥、Provider 和用量记录"
  default_prompt: "Use $dbagent-auth-config-usage-development to implement auth, settings, secrets, provider config, or usage tracking."
policy:
  allow_implicit_invocation: true
