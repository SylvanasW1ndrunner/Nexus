# 01 - 界面设计（UI Design）

> 文档版本：v0.1
> 关联：[00-overview.md](./00-overview.md)

---

## 1. 设计原则

### 1.1 三条核心准则

1. **对话优先（Conversation-first）**：主交互区是对话窗，不是 SQL 编辑器。SQL 是结果，不是入口。
2. **三栏稳定布局**：左导航（连接/会话）+ 中央工作区（对话+结果）+ 右辅助（Schema/Tool/Plan），所有界面遵循此布局，避免上下文跳跃。
3. **执行链路可追溯**：每一次 Agent 行动（思考、调用工具、执行 SQL）都有明确的视觉呈现，用户随时可以看到"它在干什么"。

### 1.2 设计语言

- **风格**：现代简洁，参考 Linear / Raycast / Claude Desktop
- **配色**：深色为主（数据工程师习惯），浅色可切换
- **字体**：UI 用 Inter / 系统默认，代码用 JetBrains Mono / Cascadia Code
- **密度**：信息密度中等偏高，类似 IDE，不做"大留白"风格

---

## 2. 总体布局

### 2.1 主窗口结构

```
┌──────────────────────────────────────────────────────────────────────┐
│  [≡] DBAgent          [连接选择 ▾]    [模式: 询问 ▾]    [👤] [⚙]    │ ← 顶部栏
├────────────┬─────────────────────────────────────┬───────────────────┤
│            │                                     │                   │
│  连接列表  │         对话工作区                  │  上下文面板        │
│  ─────────│  ┌──────────────────────────────┐   │  ┌──────────────┐ │
│  ▸ 本地PG  │  │ 用户：上周GMV最高的商品？   │   │  │ Schema 树   │ │
│  ▾ 生产PG  │  │                              │   │  │ ▸ public   │ │
│    ▸ users │  │ Agent: 我来查询...          │   │  │   ▸ users  │ │
│    ▸ orders│  │   [思考中...]               │   │  │   ▸ orders │ │
│            │  │   [→ 调用 RAG 检索]         │   │  └──────────────┘ │
│  ─────────│  │   [生成 SQL]                │   │  ┌──────────────┐ │
│  会话历史  │  │   ┌──────────────────┐     │   │  │ Tools (12)  │ │
│  ▸ 今天    │  │   │ SELECT ...        │     │   │  │ ✓ query_db  │ │
│    · 会话1 │  │   │ FROM orders ...   │     │   │  │ ✓ describe_t│ │
│    · 会话2 │  │   └──────────────────┘     │   │  │ + decrypt   │ │
│  ▸ 昨天    │  │   [✓ 执行] [✗ 取消] [✎改] │   │  └──────────────┘ │
│            │  └──────────────────────────────┘   │  ┌──────────────┐ │
│            │  ┌──────────────────────────────┐   │  │ Plan (执行) │ │
│  [+ 新连接]│  │ 输入消息...           [➤]   │   │  │ 1.✓ 检索    │ │
│            │  └──────────────────────────────┘   │  │ 2.→ 生成SQL │ │
│            │  ┌──────────────────────────────┐   │  │ 3.· 执行    │ │
│            │  │  结果表格 (虚拟滚动)         │   │  │ 4.· 解读    │ │
│            │  │  ┌──────────────────────┐    │   │  └──────────────┘ │
│            │  │  │ id │ name │ qty │... │    │   │                   │
│            │  └──────────────────────────────┘   │                   │
├────────────┴─────────────────────────────────────┴───────────────────┤
│  [状态] 已连接：生产PG · RAG: 已索引 1247 个对象 · DeepSeek-V3       │ ← 底部栏
└──────────────────────────────────────────────────────────────────────┘
```

### 2.2 三大区域职责

| 区域 | 主要职责 | 可折叠 |
|---|---|---|
| **左侧栏（Sidebar）** | 数据库连接、会话历史 | 是（默认展开 240px） |
| **中央工作区（Workspace）** | 对话 + 结果展示 | 否（核心区） |
| **右侧栏（Context Panel）** | Schema 树、Tool 列表、Plan 视图 | 是（默认展开 320px） |

### 2.3 顶部栏（Top Bar）

```
[≡ 菜单]  [DBAgent Logo]  [连接选择器 ▾]  [Agent 模式 ▾]  [搜索 🔍]  [👤 账号]  [⚙ 设置]
```

| 元素 | 行为 |
|---|---|
| `≡` 菜单 | 折叠/展开左侧栏 |
| 连接选择器 | 快速切换当前会话使用的数据库连接 |
| Agent 模式 | 询问执行 / 自动执行 / 只读模式（三选一）|
| 搜索 | 全局搜索（会话、Skill、Tool、表） |
| 账号 | 订阅信息、登出 |
| 设置 | 打开设置面板（模型、外观、快捷键等） |

### 2.4 底部栏（Status Bar）

```
[● 已连接: 生产PG]  [RAG: 1247 对象 ✓]  [模型: DeepSeek-V3]  [Token: 1.2k/40k]  [v0.1.0]
```

---

## 3. 详细交互点

### 3.1 数据库连接

#### 3.1.1 连接列表项

```
┌────────────────────────────┐
│ ● 生产 PostgreSQL    [⋮]   │  ← 状态点 + 名称 + 操作菜单
│   prod-db.company.com:5432 │
│   schemas: public, hr      │
└────────────────────────────┘
```

**状态点颜色**：
- 🟢 绿色：已连接
- 🟡 黄色：连接中 / RAG 索引中
- 🔴 红色：连接失败
- ⚪ 灰色：未连接

**右键菜单**：
- 连接 / 断开
- 编辑连接
- 重新索引 RAG
- 导出 Schema
- 删除连接

#### 3.1.2 新建连接对话框

```
┌─ 新建数据库连接 ──────────────────────────────┐
│                                              │
│  连接名称   [生产 PostgreSQL              ]  │
│  数据库类型 [PostgreSQL ▾]                    │
│                                              │
│  ── 基础 ──                                  │
│  Host       [prod-db.company.com         ]  │
│  Port       [5432  ]                         │
│  Database   [main  ]                         │
│  Username   [readonly_user               ]  │
│  Password   [••••••••••••]  [☐ 保存到 keychain] │
│                                              │
│  ── 高级 ──  [▸ 展开]                        │
│  ☐ 使用 SSH 隧道                             │
│  ☐ 启用 SSL                                  │
│  ☑ 默认只读模式                              │
│  ☑ 启动后自动建立 RAG                        │
│                                              │
│  [测试连接]                  [取消] [保存]   │
└──────────────────────────────────────────────┘
```

**测试连接** 反馈：
- 成功：绿色提示 + 显示数据库版本
- 失败：红色错误信息 + 排查建议

### 3.2 对话工作区

#### 3.2.1 消息气泡类型

| 类型 | 视觉 | 内容 |
|---|---|---|
| 用户消息 | 右对齐，蓝色背景 | 用户输入的文字 |
| Agent 思考 | 左对齐，灰色斜体，可折叠 | "正在检索 schema..." |
| Tool 调用 | 左对齐，带工具图标卡片 | 工具名 + 参数 + 结果 |
| SQL 卡片 | 左对齐，代码高亮 + 操作按钮 | SQL 内容 + 执行/编辑 |
| 结果总结 | 左对齐，markdown 渲染 | Agent 的最终回答 |
| 错误消息 | 红色边框卡片 | 错误信息 + 重试按钮 |

#### 3.2.2 SQL 卡片详细设计

```
┌─ 🗄 SQL 待执行 ────────────────── [询问模式] ─┐
│                                                │
│  -- Agent 生成 · 影响行数预估: ~1247 行       │
│  SELECT product_name, SUM(qty) AS total       │
│  FROM order_items                              │
│  WHERE created_at >= NOW() - INTERVAL '7 days'│
│  GROUP BY product_name                         │
│  ORDER BY total DESC                           │
│  LIMIT 10;                                     │
│                                                │
│  📊 EXPLAIN: Seq Scan (cost=0..156)            │
│  ⚠ 涉及表：order_items                         │
│                                                │
│  [✓ 执行] [✎ 编辑] [📋 复制] [✗ 取消]          │
└────────────────────────────────────────────────┘
```

**关键交互**：
- **编辑**：点击后切换为 Monaco 编辑器，用户可改 SQL，改完后"重新预估"
- **执行**：执行成功后卡片折叠为一行摘要："✓ 已执行 · 返回 10 行 · 234ms"
- **取消**：从 plan 中移除该步骤，agent 收到反馈后可重新规划

#### 3.2.3 Tool 调用卡片

```
┌─ 🔧 调用工具：decrypt_phone ────────────────────┐
│                                                  │
│  参数：                                          │
│    encrypted: "AES_xK9nP2..."                   │
│                                                  │
│  ▸ 展开返回值                                    │
│    "+86-138****1234"                             │
│                                                  │
│  耗时 12ms · MCP: user-tools                     │
└──────────────────────────────────────────────────┘
```

**自动执行模式下**：直接执行，结果展示
**询问模式下**：先显示参数，等待用户 [✓ 允许] / [✗ 拒绝]

#### 3.2.4 输入框

```
┌──────────────────────────────────────────────┐
│ @ 选择 Skill / 表          [📎] [🎤]        │
│ 输入消息... 上周哪个商品销量最高？           │
│                                              │
│                                       [➤]   │
└──────────────────────────────────────────────┘
   [Enter 发送 | Shift+Enter 换行 | / 命令]
```

**特殊输入**：
- `@表名`：在消息中引用具体表，会优先注入 schema
- `/skill 名称`：直接调用某个 Skill
- `/clear`：清空当前会话上下文（保留消息历史）
- `/mode auto`：临时切换到自动模式
- `/explain`：让 agent 解释上一条 SQL

#### 3.2.5 消息流式渲染

- Agent 响应采用流式（streaming）输出
- 思考过程实时打字机效果
- Tool 调用即时插入消息流
- 用户可随时点击 [⏸ 中止] 按钮停止 agent

### 3.3 结果区域

#### 3.3.1 结果面板布局

结果区域出现在对话下方，可拖拽调整高度，也可以"最大化"全屏。

```
┌─ 结果 · 10 行 · 234ms ──────── [📊 图表] [📥 导出] [⛶ 全屏] ─┐
│  ┌────┬──────────────┬────────┐                              │
│  │ #  │ product_name │ total  │                              │
│  ├────┼──────────────┼────────┤                              │
│  │ 1  │ iPhone 15    │ 1234   │                              │
│  │ 2  │ MacBook Pro  │  876   │                              │
│  │ ...│              │        │                              │
│  └────┴──────────────┴────────┘                              │
│  分页: [< 1/3 >]                                             │
└──────────────────────────────────────────────────────────────┘
```

#### 3.3.2 结果操作

| 操作 | 行为 |
|---|---|
| 列点击 | 排序 |
| 列右键 | 隐藏/显示/锁定列 |
| 单元格双击 | 查看完整值（适用于长文本/JSON） |
| 行右键 | 复制行 / 复制为 INSERT / 基于此行查询 |
| 导出 | CSV / JSON / Excel / Markdown |
| 图表 | 自动推荐图表类型（柱状/折线/饼图） |

#### 3.3.3 大数据量处理

- 结果超过 1000 行：虚拟滚动 + 分页
- 单元格内容超过 500 字符：截断 + "查看完整"
- 默认 LIMIT：自动模式下，agent 生成 SELECT 时默认追加 `LIMIT 1000`，可在设置中关闭

### 3.4 右侧上下文面板

#### 3.4.1 三个标签页

```
┌──────────────────────┐
│ [Schema][Tools][Plan]│ ← Tab 切换
├──────────────────────┤
│  (内容区)             │
│                       │
└──────────────────────┘
```

#### 3.4.2 Schema 树

```
Schema 树
─────────
🔍 [搜索表/字段...]

▾ public (12 tables)
  ▾ users (8 cols, 12.3万行)
    🔑 id          BIGINT
    📧 email       VARCHAR(255)
    🔒 phone_enc   BYTEA       ← 加密字段标记
    📅 created_at  TIMESTAMP
    ▸ Indexes (3)
    ▸ Foreign Keys (1)
  ▸ orders (10 cols, 89.5万行)
  ▸ order_items
▸ hr (4 tables)
```

**交互**：
- 双击表名：在对话框引用 `@users`
- 右键表名：查看 DDL / 查看样本数据 / 重新索引
- 字段图标含义：
  - 🔑 主键
  - 🔗 外键
  - 📧 索引字段
  - 🔒 加密字段（基于注释或自定义规则识别）
  - 📅 时间字段

#### 3.4.3 Tools 面板

```
工具箱（已启用 12 / 共 23）
─────────
[+ 添加 MCP] [🛒 浏览市场]

▾ 内置工具 (5)
  ✅ query_database
  ✅ describe_table
  ✅ explain_sql
  ✅ list_tables
  ✅ search_schema

▾ 用户 MCP: company-tools (4)
  ✅ decrypt_phone
  ✅ decrypt_id_card
  ☐ encrypt_field
  ✅ verify_signature

▾ 市场 MCP: filesystem (3)
  ☐ read_file
  ☐ write_file
  ☐ list_files
```

**交互**：
- 复选框：启用/禁用工具（禁用的工具 agent 看不到）
- 点击工具名：查看详细描述、参数、调用历史

#### 3.4.4 Plan 面板（实时执行视图）

```
执行计划 · 会话 #128
─────────
🎯 目标：分析上周 GMV 下降原因

▾ Plan
  ✅ 1. 查询本周 vs 上周订单总数
  ✅ 2. 查询本周 vs 上周客单价
  🔄 3. 查询退款率变化      ← 当前
  ⏸ 4. 综合分析输出报告

▾ 子 Agent
  🤖 Agent-A: 订单数分析 [完成]
  🤖 Agent-B: 客单价分析 [完成]
  🤖 Agent-C: 退款率分析 [运行中]

[⏸ 暂停] [⏹ 停止] [🔄 重启]
```

### 3.5 Agent 模式切换

顶部栏 [模式 ▾] 下拉菜单：

```
┌──────────────────────────────────┐
│ ● 询问后执行（推荐）              │
│   每次写操作和工具调用前确认      │
│                                  │
│ ○ 自动执行                       │
│   读操作自动执行，写操作仍需确认 │
│                                  │
│ ○ 完全自动                       │
│   ⚠ 危险，所有操作自动执行        │
│                                  │
│ ○ 只读模式                       │
│   仅允许 SELECT，禁止任何写操作   │
└──────────────────────────────────┘
```

**模式切换提示**：
- 切换到"完全自动"会弹出二次确认
- 模式状态显示在顶部栏，并且每个 SQL 卡片右上角也会显示当前模式

### 3.6 设置面板

#### 3.6.1 设置分类（左侧导航）

```
设置
────
🔧 通用
   - 语言
   - 主题（深色/浅色/跟随系统）
   - 启动行为

🤖 模型
   - LLM Provider 列表
   - 默认模型
   - Embedding 模型

🔌 数据库
   - 连接管理
   - 默认行为（自动 LIMIT、超时）

🛠 工具
   - 内置工具开关
   - MCP Server 列表
   - 公共 MCP 市场

📚 Skills
   - Skill 列表
   - 导入/导出

🎯 Agent
   - 默认模式
   - Plan 策略
   - 子 Agent 并发数
   - Token 预算

🔐 安全
   - 凭证存储
   - 危险操作清单
   - 审计日志

⌨ 快捷键
   - 自定义

ℹ 关于
   - 版本、更新、许可证
```

#### 3.6.2 模型设置详细

```
┌─ 模型设置 ────────────────────────────────────┐
│                                              │
│ LLM Providers                                │
│                                              │
│ ┌──────────────────────────────────┐ [+添加] │
│ │ ● DeepSeek           [默认]   ⚙ │         │
│ │   endpoint: api.deepseek.com    │         │
│ │   model: deepseek-chat          │         │
│ ├──────────────────────────────────┤         │
│ │ ○ OpenAI                       ⚙│         │
│ │   model: gpt-4o                 │         │
│ ├──────────────────────────────────┤         │
│ │ ○ 本地 Ollama                  ⚙│         │
│ │   endpoint: localhost:11434     │         │
│ │   model: qwen2.5-coder:32b      │         │
│ └──────────────────────────────────┘         │
│                                              │
│ Embedding Provider                           │
│ [BGE-M3 (本地) ▾]                            │
│                                              │
│ Token 预算/会话： [40000]                    │
│ ☑ 显示成本估算                               │
└──────────────────────────────────────────────┘
```

详细见 [04-config-design.md](./04-config-design.md)。

### 3.7 MCP 市场

#### 3.7.1 浏览界面

```
┌─ MCP 市场 ──────────────────────────────────────┐
│                                                  │
│ [🔍 搜索...]   [分类 ▾]  [来源: Smithery ▾]      │
│                                                  │
│ ┌─────────────────────────────────────────────┐ │
│ │ 📁 Filesystem                       [安装]  │ │
│ │ ⭐ 4.8  ↓ 12k   官方                        │ │
│ │ 文件系统读写操作                             │ │
│ ├─────────────────────────────────────────────┤ │
│ │ 🌐 Fetch                            [已安装] │ │
│ │ ⭐ 4.6  ↓ 8.5k                              │ │
│ │ HTTP 请求                                    │ │
│ ├─────────────────────────────────────────────┤ │
│ │ 🐙 GitHub                           [安装]  │ │
│ │ ⭐ 4.7  ↓ 6.2k                              │ │
│ │ 操作 GitHub 仓库                             │ │
│ └─────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────┘
```

**安装流程**：
1. 点击 [安装] → 弹出权限说明
2. 用户确认 → 后台执行（npm install / docker pull / ...）
3. 自动出现在 Tools 面板"市场 MCP"分组

### 3.8 会话管理

#### 3.8.1 会话列表（左侧栏底部）

```
会话历史
────────
[+ 新会话]  [🔍 搜索]

▾ 今天
  💬 GMV 下降分析            14:23
  💬 用户表数据清洗           10:15
▾ 昨天
  💬 性能慢查询排查           18:42
  💬 月度报表 SQL             09:33
▾ 更早
  ▸ 本周 (12)
  ▸ 本月 (47)
```

**右键菜单**：
- 重命名
- 导出（Markdown / JSON）
- 分享（生成只读链接，可选）
- 归档
- 删除

#### 3.8.2 会话切换

点击会话项：
- 切换到该会话的 context
- 切换到该会话使用的连接
- 恢复对话历史和 Plan 状态

---

## 4. 关键状态与反馈

### 4.1 全局加载状态

| 状态 | 视觉 |
|---|---|
| Agent 思考中 | 顶部进度条 + Agent 头像跳动 |
| RAG 索引中 | 底部状态栏显示进度 "索引中 234/1247" |
| SQL 执行中 | SQL 卡片显示 spinner + "可中止" 按钮 |
| MCP 调用中 | Tool 卡片显示 spinner |

### 4.2 错误处理

| 错误类型 | 视觉 | 行为 |
|---|---|---|
| 数据库连接失败 | 顶部红色 banner | 提示重连 |
| SQL 语法错误 | SQL 卡片标红 + 错误指示器 | Agent 自动尝试修复（最多 2 次） |
| LLM API 失败 | 消息气泡红框 | 提示切换模型 |
| MCP 调用失败 | Tool 卡片红框 | Agent 决定继续或放弃 |
| Token 超限 | 输入框上方 banner | 提示压缩历史 |

### 4.3 危险操作确认

```
┌─ ⚠ 确认执行危险操作 ────────────────┐
│                                    │
│  以下 SQL 将影响 ~12,000 行数据：  │
│                                    │
│  DELETE FROM users WHERE ...      │
│                                    │
│  ☐ 我已检查 SQL 内容               │
│  ☐ 当前为生产环境，我已知晓风险    │
│                                    │
│           [取消] [确认执行]        │
└────────────────────────────────────┘
```

危险操作清单（可在设置中编辑）：
- DROP / TRUNCATE
- DELETE / UPDATE 不带 WHERE
- ALTER TABLE
- 影响行数 > 1000
- 用户标记为"生产"的连接上的写操作

---

## 5. 快捷键

| 快捷键 | 功能 |
|---|---|
| `Cmd/Ctrl + N` | 新会话 |
| `Cmd/Ctrl + K` | 全局命令面板 |
| `Cmd/Ctrl + L` | 聚焦输入框 |
| `Cmd/Ctrl + /` | 切换 Agent 模式 |
| `Cmd/Ctrl + B` | 切换左侧栏 |
| `Cmd/Ctrl + J` | 切换右侧栏 |
| `Cmd/Ctrl + Enter` | 发送消息 |
| `Esc` | 中止 agent / 取消当前 SQL |
| `Cmd/Ctrl + ,` | 打开设置 |
| `Cmd/Ctrl + Shift + R` | 重新索引 RAG |

---

## 6. 响应式与多窗口

### 6.1 窗口尺寸

- 最小：1280 × 800
- 推荐：1920 × 1080
- 小屏自适应：< 1440 时右侧栏默认折叠

### 6.2 多窗口

- 支持同时打开多个会话窗口
- 不同窗口可连接不同数据库
- 窗口间共享：连接、Skills、Tools、Settings
- 窗口间不共享：当前会话 context

---

## 7. 可访问性（A11y）

- 所有交互元素支持键盘导航（Tab / Arrow keys）
- 颜色对比度满足 WCAG AA
- 提供高对比度主题
- 屏幕阅读器友好（aria-label）
- 字号可调（设置中提供 small / medium / large）

---

## 8. 待定项

- [ ] 是否需要"Notebook 模式"（类似 Jupyter，多 cell 混排 SQL/对话）
- [ ] ER 图可视化是否进 MVP
- [ ] 是否做"协作"（多人共享会话）
- [ ] 移动端伴侣 App（仅查看，不操作）
