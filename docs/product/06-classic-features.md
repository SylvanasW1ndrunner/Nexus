# 06 - 传统数据库工具功能（Classic Features）

> 文档版本：v0.1
> 关联：[00-overview.md](./00-overview.md), [01-ui-design.md](./01-ui-design.md)

---

## 1. 为什么必须做"传统功能"

### 1.1 一个核心判断

> **AI 是叠加值，传统能力是基本盘。** Cursor 之所以能取代 VS Code，前提是它本身就是一个完整可用的编辑器；如果它只能 chat，没人会用。

DBAgent 同理：
- 数据工程师 80% 的日常操作是**确定性**的：连接、看表、改字段、跑 SQL、导数据
- 这些操作用对话来做反而**慢且不稳定**
- 用户在熟练的操作上不需要"AI 帮忙"，需要"工具不挡路"
- AI 的价值在剩下 20% 的"探索性、复杂、不熟"场景

### 1.2 "AI + 传统"的边界

| 场景 | 用什么 |
|---|---|
| "把第三行的 status 改成 paid" | 双击单元格直接改（传统） |
| "新建一个 users 表，含 id/email/created_at" | 表设计器（传统） |
| "查 users 表前 100 行" | 双击表名（传统） |
| "导出查询结果到 Excel" | 工具栏按钮（传统） |
| "上周哪个商品销量最高" | Chat（AI） |
| "为什么 GMV 下降" | Chat（AI 多步分析） |
| "把 phone 解密后按城市聚合" | Chat（AI + 工具调用） |

**设计原则**：能用 1 次点击/双击/快捷键完成的，不要让用户输入文字让 AI 做。

---

## 2. 设计原则（贯穿全文）

### 2.1 三条铁律

1. **每个 AI 能做的事，传统操作也要能做**：用户可以选择是否用 AI，永不强制
2. **传统操作要快**：键盘党、右键党都要照顾，命令面板覆盖所有操作
3. **AI 的输出要回流到传统视图**：Agent 生成的 SQL 可以"打开为编辑器 tab"继续手改

### 2.2 与设计宗旨的对齐

| 宗旨 | 在传统功能上的体现 |
|---|---|
| 轻量化 | MVP 只做高频操作，重型功能（数据同步、调度）后置 |
| 模块化 | 每个传统功能是独立 panel/view，可单独迭代 |
| 简洁科技感 | 学习 Postman：清晰分区、克制配色、信息密度合理 |
| 用户体验为先 | 快捷键齐全、撤销机制、错误友好提示 |

---

## 3. 整体形态：双模工作区

### 3.1 Tab 系统（类 Cursor / Postman）

工作区采用**多 Tab 设计**，每个 Tab 是一个独立"文档"：

```
┌────────────────────────────────────────────────────────────────────┐
│  [💬 GMV 分析] [📄 query.sql] [📊 users 数据] [🔧 users 设计] [+] │ ← Tab 栏
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│            （当前 Tab 的内容区）                                    │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

**Tab 类型**：

| 图标 | 类型 | 描述 |
|---|---|---|
| 💬 | Chat | AI 对话会话 |
| 📄 | SQL Editor | SQL 编辑器（可关联连接） |
| 📊 | Table Data | 表数据浏览/编辑 |
| 🔧 | Table Designer | 表结构设计 |
| 🗺 | ER Diagram | ER 图查看 |
| ⚙ | Procedure | 存储过程/函数编辑 |
| 👁 | View | 视图定义 |
| 🔍 | Query Plan | EXPLAIN 结果可视化 |
| 📋 | Result Snapshot | 历史结果快照 |

**Tab 行为**：
- 拖拽重排
- `Cmd+W` 关闭，`Cmd+Shift+T` 撤销关闭
- 右键菜单：关闭/关闭其他/锁定/复制
- 修改未保存时图标显示 `●`
- 双击表名 → 在新 Tab 打开（默认数据 Tab）
- `Cmd+P` 快速切换 Tab（模糊搜索）

### 3.2 Chat Panel（始终可调出）

Chat 不是 Tab，而是**右侧面板**或**底部抽屉**（用户可选）：

```
┌─────────────────────────────┬──────────────────┐
│                             │                  │
│   主工作区（Tab 内容）      │   Chat Panel    │
│                             │   （可拉宽）     │
│                             │                  │
└─────────────────────────────┴──────────────────┘
```

**关键交互**：
- `Cmd+L` 切换 Chat 面板显示/隐藏
- 在任何 Tab 中按 `Cmd+K` 唤起内嵌 AI 输入框（不切走当前视图）
- Chat 中的 SQL 卡片右上角有 [📤 打开为编辑器]，点击后在新 Tab 打开
- 选中编辑器中的 SQL，按 `Cmd+L` → 自动以选中内容作为上下文打开 Chat

### 3.3 默认布局

```
┌──────────────────────────────────────────────────────────────────────┐
│  [≡] DBAgent      [连接 ▾]   [模式 ▾]    [搜索]    [👤] [⚙]        │
├──────┬───────────────────────────────────────────────┬──────────────┤
│      │ Tab Bar                                       │              │
│ 左侧 ├───────────────────────────────────────────────┤              │
│ 导航 │                                               │  Chat Panel  │
│      │              主工作区                          │  (可隐藏)    │
│      │                                               │              │
│      │                                               │              │
│      ├───────────────────────────────────────────────┤              │
│      │ 命令输出 / 结果区（视 Tab 类型而定）           │              │
└──────┴───────────────────────────────────────────────┴──────────────┘
```

---

## 4. 左侧导航（Sidebar）

### 4.1 三个标签页

```
┌─────────────────────┐
│ [🔌][📁][🕐]        │ ← 标签切换
├─────────────────────┤
│                     │
│  当前面板内容        │
│                     │
└─────────────────────┘
```

| 图标 | 面板 | 内容 |
|---|---|---|
| 🔌 | 连接 | 连接列表 + Schema 树 |
| 📁 | 文件 | 本地 SQL 文件管理 |
| 🕐 | 历史 | 会话历史 + SQL 历史 |

### 4.2 连接面板（最重要）

```
连接 [+]
├─ ● 本地 PG (dev)
│  └─ ▾ public
│     ├─ ▾ Tables (12)
│     │  ├─ users          [表图标]
│     │  ├─ orders         [表图标]
│     │  └─ order_items    [表图标]
│     ├─ ▸ Views (3)
│     ├─ ▸ Materialized Views (1)
│     ├─ ▸ Functions (8)
│     ├─ ▸ Procedures (2)
│     ├─ ▸ Sequences (5)
│     └─ ▸ Indexes (15)
├─ ● 生产 PG (prod) [🔒只读]
└─ ○ 测试库
```

**节点类型与右键菜单**：

#### 连接节点
- 连接 / 断开
- 编辑连接
- 新建查询（Cmd+N）
- 重新加载
- 重建 RAG 索引
- 导出 schema (DDL / Markdown)
- 删除

#### 数据库 / Schema 节点
- 新建查询（默认 use 该 schema）
- 新建表
- 新建视图
- 新建函数

#### 表节点（最高频）
- **打开数据**（默认双击行为）
- **设计表**
- **查看 DDL**
- 新建查询（自动 SELECT * FROM ...）
- 复制表名 / 复制 INSERT 模板 / 复制 SELECT 模板
- 重命名
- 截断（TRUNCATE）
- 删除（DROP）
- 导出 → CSV / JSON / Excel / SQL
- 导入数据
- 在 Chat 中引用此表（@users）

#### 字段节点（展开表后）
- 复制名称
- 编辑字段
- 删除字段
- 添加索引
- 标记为加密（关联 RAG）

### 4.3 智能搜索

顶部 [🔍 搜索连接和对象] 输入框：
- 实时搜索表名、字段名、连接名
- 支持模糊匹配
- 结果按"连接 > schema > 表"分组
- `Cmd+T` 全局唤起

### 4.4 Schema 树性能

- **懒加载**：连接时不立即加载所有对象，展开时再拉
- **虚拟滚动**：表数 > 100 时启用
- **缓存**：从 RAG 元数据库读，比直接查 PG 快
- **变更检测**：DDL 后自动刷新（PG LISTEN/NOTIFY）

---

## 5. SQL 编辑器（Tab）

### 5.1 整体布局

```
┌────────────────────────────────────────────────────────────────────┐
│  连接: 本地PG ▾  |  数据库: postgres ▾  |  Schema: public ▾       │
├────────────────────────────────────────────────────────────────────┤
│  [▶ 运行] [▶ 选中] [⏸ 取消] [💾 保存] [📋 格式化] [🤖 AI 改写]    │
├────────────────────────────────────────────────────────────────────┤
│  1  SELECT u.id, u.email, COUNT(o.id) AS order_count               │
│  2  FROM users u                                                   │
│  3  LEFT JOIN orders o ON o.user_id = u.id                         │
│  4  WHERE u.created_at >= NOW() - INTERVAL '30 days'               │
│  5  GROUP BY u.id, u.email                                         │
│  6  ORDER BY order_count DESC                                      │
│  7  LIMIT 100;                                                     │
│                                                                    │
├────────────────────────────────────────────────────────────────────┤
│  [📊 结果 1] [⚠ 消息] [📝 历史] [🗺 执行计划]                      │
├────────────────────────────────────────────────────────────────────┤
│  ┌──────┬─────────────────────┬──────────────┐                    │
│  │ id   │ email               │ order_count  │                    │
│  ├──────┼─────────────────────┼──────────────┤                    │
│  │ 1234 │ alice@example.com   │ 12           │                    │
│  └──────┴─────────────────────┴──────────────┘                    │
│  100 rows · 234ms · [⬇ 导出] [📊 图表] [📌 钉住]                  │
└────────────────────────────────────────────────────────────────────┘
```

### 5.2 编辑器能力（Monaco Editor）

| 能力 | 说明 |
|---|---|
| 语法高亮 | PG/MySQL 方言识别 |
| 自动完成 | 关键字、表名、字段名（来自 RAG 元数据） |
| 智能提示 | 输入 `users.` 自动提示字段 |
| 错误标注 | 实时校验语法（轻量级，不替代执行） |
| 格式化 | sql-formatter，可配置缩进/大小写规则 |
| 多光标 | Cmd+Click |
| 代码折叠 | 大 SQL 友好 |
| 查找替换 | Cmd+F / Cmd+Shift+F |
| Vim 模式 | 可选（Monaco vim 插件） |
| 主题 | 跟随全局主题 |
| 字体 | 等宽（JetBrains Mono / Cascadia Code 等） |
| 历史 | 文件级 Undo/Redo |

### 5.3 执行模式

| 操作 | 行为 | 快捷键 |
|---|---|---|
| 运行全部 | 执行整个文件 | Cmd+Enter |
| 运行选中 | 仅执行选中文本 | Cmd+Shift+Enter |
| 运行光标处语句 | 自动识别 `;` 分隔的当前语句 | Cmd+E |
| 运行并 EXPLAIN | EXPLAIN ANALYZE 当前语句 | Cmd+Shift+E |
| 取消执行 | 中断长查询 | Esc |

**关键交互**：
- 长查询执行时，编辑器顶部出现 progress bar + 取消按钮
- 多语句 SQL 按 `;` 分割，结果区出现多个标签页（结果1/结果2/...）
- 错误信息精确定位到行号 + 高亮

### 5.4 AI 改写（关键的 AI/传统融合点）

编辑器选中一段 SQL → 工具栏 [🤖 AI 改写] 或 `Cmd+K`：

```
┌─ AI 改写 ──────────────────────────────┐
│ 选中: SELECT * FROM orders WHERE ...  │
│                                        │
│ 提示: [让查询用上索引                ] │
│                                        │
│ [✓ 应用] [✗ 取消] [♻ 重生成]          │
│                                        │
│ ── Diff 预览 ──                        │
│ - SELECT * FROM orders WHERE ...      │
│ + SELECT id, status FROM orders        │
│ +   WHERE created_at >= '2026-01-01' │
└────────────────────────────────────────┘
```

**特征**：
- 不离开当前编辑器
- Diff 视图直接可见
- 接受/拒绝快捷
- 这是"AI 增强传统"的标杆交互

### 5.5 SQL 文件管理

- 编辑器 Tab 可保存为 `.sql` 文件到本地
- 左侧"文件"标签页管理本地 SQL 文件
- 文件可关联默认连接（保存在文件 metadata 中）
- 支持新建文件夹组织
- 可设置默认工作区目录

### 5.6 SQL 片段（Snippets）

- 内置常用片段：`sel`、`ins`、`upd`、`del`、`cre-table`、`cre-idx` 等
- 用户可自定义片段（保存在 `~/.dbagent/snippets/`）
- Tab 键触发展开

### 5.7 历史与回滚

- 所有执行过的 SQL 自动记录到 `query_history` 表
- 编辑器底部 [📝 历史] 标签查看本编辑器的执行历史
- 全局 SQL 历史在左侧"历史"面板
- 支持搜索、复制、重新执行

---

## 6. 表数据浏览（Tab）

### 6.1 整体布局

```
┌────────────────────────────────────────────────────────────────────┐
│  📊 users · 本地PG/public · 12,345 rows                            │
│  [🔄] [⚙] [🔍 筛选] [↕ 排序] [👁 列] [+ 新增] [📥 导入] [⬇ 导出]  │
├────────────────────────────────────────────────────────────────────┤
│  ▾ WHERE: status = 'active'  AND created_at > '2026-01-01'        │ ← 筛选条
├────────────────────────────────────────────────────────────────────┤
│  ┌────┬────────┬─────────────────┬────────────┬──────────────┐    │
│  │ #  │ id 🔑  │ email          │ status     │ created_at   │    │
│  ├────┼────────┼─────────────────┼────────────┼──────────────┤    │
│  │ 1  │ 1001   │ alice@e.com    │ active     │ 2026-01-15   │    │
│  │ 2  │ 1002   │ bob@e.com      │ inactive   │ 2026-01-16   │    │
│  │ 3  │ 1003   │ ●●●●●● (改)    │ active     │ 2026-01-17   │ ← 修改未提交
│  └────┴────────┴─────────────────┴────────────┴──────────────┘    │
├────────────────────────────────────────────────────────────────────┤
│  ⚠ 1 行待提交   [👁 预览 SQL] [✓ 提交] [↶ 回退]   ◀ 1/100 ▶      │
└────────────────────────────────────────────────────────────────────┘
```

### 6.2 加载策略

- **分页加载**：默认每页 100 行，可调
- **虚拟滚动**：滚动到底部自动加载下一页（可选无限滚动）
- **大表优化**：> 10 万行强制分页 + 提示
- **首次打开**：自动加 `LIMIT 100`，避免拖死大表

### 6.3 筛选与排序

#### 筛选
- 列头 [🔍] 图标点击 → 下拉筛选器
- 筛选条件可视化展示在工具栏下方"WHERE"条
- 支持 `=`, `!=`, `>`, `<`, `IN`, `LIKE`, `IS NULL`, `BETWEEN` 等
- 多条件 AND 组合
- 高级模式：直接输入 SQL WHERE 子句

#### 排序
- 列头点击循环：升序 → 降序 → 取消
- 多列排序：Shift+点击
- 排序状态可视化（▲▼ 图标）

### 6.4 列管理

- **显示/隐藏列**：[👁 列] 弹出复选框列表
- **列宽调整**：拖拽列边
- **列固定**：右键列 → 固定到左侧
- **列重排**：拖拽列头
- **列宽自适应**：双击列边自动适配内容

### 6.5 单元格编辑（关键能力）

#### 编辑模式

| 列类型 | 编辑方式 |
|---|---|
| TEXT/VARCHAR | 双击 → 单行输入 / 长文本弹窗 |
| INTEGER/NUMERIC | 双击 → 数字输入 + 校验 |
| BOOLEAN | 双击 → 复选框 |
| DATE/TIMESTAMP | 双击 → 日期选择器 |
| ENUM | 双击 → 下拉选择 |
| JSON/JSONB | 双击 → 内嵌 JSON 编辑器 |
| ARRAY | 双击 → 数组编辑器 |
| BYTEA | 仅展示 hex，不允许编辑（需用 SQL） |
| 加密字段 | 提示"加密字段，需通过 Tool 编辑" |

#### 编辑状态

- 修改后单元格显示 `●` 标记 + 黄色背景
- 行号区显示 `M`（modified）/ `+`（new）/ `-`（deleted）
- 工具栏出现"待提交：N 行"提示

#### 事务保护流程

```
1. 用户编辑多个单元格 / 行
   ↓
2. 工具栏 [👁 预览 SQL] 显示生成的 UPDATE/INSERT/DELETE 语句
   ↓
3. 用户点 [✓ 提交]
   ↓
4. 弹出确认对话框：
   - 显示完整 SQL
   - 显示影响行数
   - 风险评级
   ↓
5. 用户确认 → 在事务中执行
   ↓
6. 成功 → 提示 + 刷新数据
   失败 → 自动 ROLLBACK + 错误提示 + 编辑状态保留
```

**关键安全设计**：
- 没有主键的表禁止行编辑（仅允许 INSERT）
- WHERE 子句必须基于主键（避免误更新多行）
- 超过 N 行（默认 50）的批量编辑强制确认两次

### 6.6 行操作（右键菜单）

```
- 复制行
- 复制为 INSERT
- 复制为 JSON
- 复制单元格值
- 基于此行查询（生成 SELECT WHERE pk=...）
- 在 Chat 中引用此行
- 删除行
```

### 6.7 大字段查看

- 单元格内容超过 100 字符截断 + `...`
- 双击进入"查看模式"（不进入编辑）
- 弹窗显示完整内容
- JSON 自动格式化高亮
- 长文本支持搜索

### 6.8 导出

工具栏 [⬇ 导出]：

| 格式 | 选项 |
|---|---|
| CSV | 分隔符、编码、引号策略 |
| Excel (.xlsx) | 多 sheet（结果 + 元信息） |
| JSON | 数组 / 一行一对象 |
| SQL (INSERT) | 含/不含表结构 |
| Markdown | 表格格式 |

- 范围选择：当前页 / 筛选结果 / 全表
- 大数据量：流式导出 + 进度条

---

## 7. 表设计器（Tab）

### 7.1 整体布局

```
┌────────────────────────────────────────────────────────────────────┐
│  🔧 users · 本地PG/public                                          │
│  [💾 保存] [👁 预览 DDL] [↶ 撤销] [↷ 重做] [🗑 删除表]              │
├────────────────────────────────────────────────────────────────────┤
│  [字段] [索引] [外键] [触发器] [注释] [DDL]                         │ ← Tab
├────────────────────────────────────────────────────────────────────┤
│  ┌──┬─────────┬──────────┬────────┬──────┬──────────┬─────────┐  │
│  │  │ 字段名   │ 类型     │ 长度   │ NULL │ 默认     │ 注释    │  │
│  ├──┼─────────┼──────────┼────────┼──────┼──────────┼─────────┤  │
│  │🔑│ id      │ BIGINT   │        │ NO   │ identity │ 主键    │  │
│  │  │ email   │ VARCHAR  │ 255    │ NO   │          │ 邮箱    │  │
│  │+ │ phone   │ VARCHAR  │ 32     │ YES  │ NULL     │         │ ← 新增
│  └──┴─────────┴──────────┴────────┴──────┴──────────┴─────────┘  │
│  [+ 添加字段]                                                       │
├────────────────────────────────────────────────────────────────────┤
│  ⚠ 未保存：1 个新字段                                              │
└────────────────────────────────────────────────────────────────────┘
```

### 7.2 字段编辑

每行字段支持：
- 名称、类型、长度、精度、NULL、默认值、注释
- 是否主键（多字段联合主键）
- 是否唯一
- 自增/IDENTITY
- 检查约束（CHECK）
- 字符集/排序规则

### 7.3 索引面板

```
┌──────────────────────────────────────────────────────┐
│  名称              字段           类型      唯一    │
│  ─────────────────────────────────────────────────  │
│  pk_users          (id)           btree     ✓ PK   │
│  uk_users_email    (email)        btree     ✓      │
│  ix_users_status   (status)       btree            │
│  ix_users_created  (created_at)   btree            │
│  [+ 新增索引]                                       │
└──────────────────────────────────────────────────────┘
```

支持 PG 索引类型：btree / hash / gin / gist / brin

### 7.4 外键面板

```
┌──────────────────────────────────────────────────────────────┐
│ 名称              本地字段    引用表.字段     ON DELETE  ON UPDATE │
│ ─────────────────────────────────────────────────────────────│
│ fk_orders_user   user_id      users.id       CASCADE     RESTRICT│
└──────────────────────────────────────────────────────────────┘
```

### 7.5 DDL 预览（核心安全机制）

**所有变更不直接执行**，先生成 DDL 让用户预览：

```
┌─ 预览 DDL ───────────────────────────────────────────┐
│                                                      │
│  ALTER TABLE users                                  │
│    ADD COLUMN phone VARCHAR(32) NULL;               │
│                                                      │
│  COMMENT ON COLUMN users.phone IS '...';            │
│                                                      │
│  ☐ 我已检查 DDL                                      │
│                                                      │
│  [取消] [复制 DDL] [✓ 执行]                          │
└──────────────────────────────────────────────────────┘
```

### 7.6 新建表

新建表用同一设计器，左上角名称为空 + "新建表" 标识：
- 至少需要 1 个字段
- 强制提示设置主键（可跳过）
- 保存时生成 CREATE TABLE 语句

### 7.7 修改与回滚

- 设计器有完整的 undo/redo 栈
- 提交后才真正执行 SQL
- 失败的 ALTER 自动 ROLLBACK
- 设计器中可"导出当前设计为 SQL 文件"备份

---

## 8. ER 图查看（Tab）

### 8.1 形态：Mermaid 自动生成 + 渲染

> **不做拖拽式 ER 编辑器**（工作量大），仅做查看。
> 用 mermaid `erDiagram` 自动从 schema 元数据生成。

```
┌────────────────────────────────────────────────────────────────────┐
│  🗺 ER 图 · 本地PG · public                                        │
│  [🔄 刷新] [📥 导出 SVG] [📥 导出 mermaid] [⚙ 选择表]              │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│        ┌─────────┐         ┌──────────┐                           │
│        │ users   │ 1     n │ orders   │                           │
│        │ ─────   │─────────│ ─────    │                           │
│        │ id PK   │         │ id PK    │                           │
│        │ email   │         │ user_id  │                           │
│        │ phone   │         │ amount   │                           │
│        └─────────┘         └────┬─────┘                           │
│                                  │ 1                              │
│                                  │                                │
│                              n   ▼                                │
│                            ┌──────────────┐                       │
│                            │ order_items  │                       │
│                            │ ──────       │                       │
│                            │ id PK        │                       │
│                            │ order_id     │                       │
│                            │ product_id   │                       │
│                            │ qty          │                       │
│                            └──────────────┘                       │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

### 8.2 生成逻辑

```typescript
function generateERDiagram(tables: TableNode[], relations: RelationNode[]): string {
  const lines = ['erDiagram'];

  for (const t of tables) {
    lines.push(`  ${t.name} {`);
    for (const c of t.columns.slice(0, 10)) {  // 只显示前 10 字段
      const flags = [c.isPK && 'PK', c.isFK && 'FK'].filter(Boolean).join(',');
      lines.push(`    ${c.dataType} ${c.name} ${flags}`);
    }
    lines.push('  }');
  }

  for (const r of relations) {
    const sym = r.relationType === 'one-to-many' ? '||--o{' : '||--||';
    lines.push(`  ${r.fromTable} ${sym} ${r.toTable} : "${r.constraintName}"`);
  }

  return lines.join('\n');
}
```

### 8.3 交互

- 点击表节点 → 高亮 + 在右侧详情区显示完整字段
- 双击表节点 → 在新 Tab 打开数据
- 选择部分表显示（Schema 太大时）
- 缩放 / 平移
- 导出 SVG / PNG / mermaid 文本

### 8.4 局限性（明示）

- 不支持拖拽编辑（v1.0 不做）
- 不支持手动布局（用 mermaid 默认布局）
- 表数 > 30 时建议切到"按引用关系子图"

---

## 9. 视图、存储过程、函数

### 9.1 统一编辑器形态

视图 / 函数 / 存储过程都是**带语法高亮的 SQL Tab**，附加：
- 顶部元信息条（类型、所有者、参数）
- 工具栏 [💾 保存] [👁 预览 DDL] [▶ 测试调用]（仅函数/过程）

### 9.2 视图编辑

```
┌────────────────────────────────────────────────────────────────────┐
│  👁 view_active_users · 本地PG/public                              │
│  [💾 保存] [👁 预览] [▶ 查询此视图] [🗑 删除]                      │
├────────────────────────────────────────────────────────────────────┤
│  CREATE OR REPLACE VIEW view_active_users AS                       │
│  SELECT u.* FROM users u                                           │
│  WHERE EXISTS (                                                    │
│    SELECT 1 FROM orders o                                          │
│    WHERE o.user_id = u.id                                          │
│      AND o.created_at >= NOW() - INTERVAL '30 days'                │
│  );                                                                │
└────────────────────────────────────────────────────────────────────┘
```

### 9.3 函数 / 存储过程编辑

```
┌────────────────────────────────────────────────────────────────────┐
│  ⚙ get_user_stats(user_id BIGINT) RETURNS TABLE(...)               │
│  [💾 保存] [👁 预览] [▶ 测试] [🗑 删除]                            │
├────────────────────────────────────────────────────────────────────┤
│  CREATE OR REPLACE FUNCTION get_user_stats(uid BIGINT)             │
│  RETURNS TABLE (                                                   │
│    order_count INT,                                                │
│    total_amount NUMERIC                                            │
│  ) AS $$                                                           │
│  BEGIN                                                             │
│    RETURN QUERY                                                    │
│      SELECT COUNT(*)::INT, COALESCE(SUM(amount), 0)               │
│      FROM orders WHERE user_id = uid;                             │
│  END;                                                              │
│  $$ LANGUAGE plpgsql;                                              │
└────────────────────────────────────────────────────────────────────┘
```

### 9.4 测试调用对话框

```
┌─ 测试调用：get_user_stats ──────────────┐
│                                         │
│  参数:                                  │
│    uid (BIGINT) [1234        ]          │
│                                         │
│  [取消] [▶ 调用]                        │
└─────────────────────────────────────────┘
```

调用后结果显示在 Tab 底部结果区，与 SQL 编辑器一致。

---

## 10. 索引管理

### 10.1 入口

- 表设计器 → 索引 Tab
- 左侧 Schema 树 → Indexes 节点
- 独立索引管理 Tab（按需）

### 10.2 索引列表

```
┌────────────────────────────────────────────────────────────────────┐
│  索引列表 · 本地PG · public                                         │
│  [+ 新建] [🔄 刷新] [🔍 搜索]                                       │
├────────────────────────────────────────────────────────────────────┤
│  名称              | 表        | 字段        | 类型  | 唯一 | 大小 │
│  pk_users          | users     | (id)        | btree | ✓PK  | 12MB │
│  uk_users_email    | users     | (email)     | btree | ✓    | 8MB  │
│  ix_orders_user    | orders    | (user_id)   | btree |      | 24MB │
│  ix_orders_created | orders    | (created_at)| btree |      | 18MB │
└────────────────────────────────────────────────────────────────────┘
```

### 10.3 新建索引对话框

```
┌─ 新建索引 ──────────────────────────────────┐
│  表       [users ▾]                         │
│  名称     [ix_users_status                ] │
│  类型     [btree ▾]  (btree/hash/gin/gist) │
│  字段     [+] status [+] created_at         │
│  唯一     ☐                                 │
│  WHERE    [(可选) status = 'active'      ]  │
│  并发创建 ☑ CONCURRENTLY                    │
│                                             │
│  [👁 DDL] [取消] [✓ 创建]                   │
└─────────────────────────────────────────────┘
```

---

## 11. 用户与权限管理

### 11.1 入口

设置面板 → 数据库工具 → 用户与权限（仅在连接到支持的数据库时启用）

### 11.2 用户列表

```
┌────────────────────────────────────────────────────────────────────┐
│  PG 用户与角色                                                      │
│  [+ 新建] [🔄]                                                      │
├────────────────────────────────────────────────────────────────────┤
│  名称        | 角色 | SUPERUSER | 创建数据库 | 登录 | 角色成员 │   │
│  postgres   | 角色 | ✓        | ✓         | ✓   | -        │   │
│  app_user   | 角色 |          |           | ✓   | readonly │   │
│  readonly   | 组   |          |           |     | -        │   │
└────────────────────────────────────────────────────────────────────┘
```

### 11.3 权限授予

选中用户 / 角色 → 权限 Tab：

```
┌────────────────────────────────────────────────────────────────────┐
│  app_user 的权限                                                    │
├────────────────────────────────────────────────────────────────────┤
│  Schema: [public ▾]                                                 │
│                                                                    │
│  对象           | SELECT | INSERT | UPDATE | DELETE | TRUNCATE     │
│  users          |   ✓    |   ✓    |   ✓    |        |              │
│  orders         |   ✓    |   ✓    |   ✓    |   ✓    |              │
│  order_items    |   ✓    |        |        |        |              │
│                                                                    │
│  [+ 授权新对象] [👁 预览 GRANT 语句]                                │
└────────────────────────────────────────────────────────────────────┘
```

### 11.4 安全提示

- 危险操作（如 GRANT SUPERUSER）需双重确认
- 所有变更生成 GRANT/REVOKE 预览
- 修改前自动备份当前权限快照

---

## 12. 查询计划可视化（EXPLAIN）

### 12.1 触发方式

- SQL 编辑器右键 → "EXPLAIN"
- 工具栏 [🔍 计划] 按钮
- `Cmd+Shift+E` 快捷键
- AI 在询问"为什么这条 SQL 慢"时也会调用此功能

### 12.2 计划视图

支持两种视图，用户可切换：

#### 12.2.1 树形视图

```
┌──────────────────────────────────────────────────────────────┐
│  Sort  (cost=1234..1240 rows=12 width=64) actual=15ms       │
│  └─ Hash Join  (cost=...) actual=12ms                       │
│     ├─ Seq Scan on users  ⚠ no index used                   │
│     │  Filter: status = 'active'                            │
│     │  Rows Removed: 8523                                   │
│     └─ Hash                                                 │
│        └─ Index Scan on orders  using ix_orders_user        │
└──────────────────────────────────────────────────────────────┘
```

- 高耗时节点高亮（红色）
- 缺失索引等问题加 ⚠ 警告
- 点击节点展开详细 metrics

#### 12.2.2 火焰图视图（可选）

类似 PostgreSQL pg_stat_statements 的火焰图，用 D3 / 类似库渲染。

### 12.3 AI 解读按钮

[🤖 让 AI 解读] 按钮：把 EXPLAIN 结果传给 agent，输出"问题诊断 + 优化建议"。这是 AI/传统融合点。

---

## 13. 数据导入/导出

### 13.1 导入向导

入口：表右键 → 导入数据

```
步骤 1: 选择源
  - CSV / Excel / JSON / SQL
  - 文件路径或拖拽

步骤 2: 解析配置
  - 编码、分隔符、引号
  - 第一行是否为表头
  - 预览前 10 行

步骤 3: 字段映射
  - 源列 → 目标列
  - 类型转换
  - 默认值 / 跳过空值

步骤 4: 执行选项
  - INSERT / UPSERT / TRUNCATE 后插入
  - 批大小（默认 1000）
  - 错误处理：跳过 / 中止
  - 事务模式：单事务 / 分批事务

步骤 5: 执行
  - 进度条
  - 实时日志
  - 完成后给出总结：成功 N 行，失败 M 行
```

### 13.2 导出（已在 §6.8 描述）

补充：
- 导出可保存为"导出方案"模板，下次一键复用
- 大数据量流式导出，避免内存溢出

---

## 14. 历史记录

### 14.1 SQL 历史

所有执行过的 SQL 自动记录到本地：

```
┌────────────────────────────────────────────────────────────────────┐
│  SQL 历史 · 全部连接                                                │
│  [🔍 搜索] [筛选: 连接 ▾ | 时间 ▾ | 状态 ▾] [🗑 清空]              │
├────────────────────────────────────────────────────────────────────┤
│  2026-05-20 14:23  本地PG  ✓ 234ms                                  │
│   SELECT u.id, u.email FROM users u WHERE ...                      │
│   [▶ 重新执行] [📤 打开为编辑器] [📋 复制]                          │
│  ─────────────────────────────────────────────────                 │
│  2026-05-20 14:18  本地PG  ✗ syntax error                           │
│   SELEC * FROM users                                                │
└────────────────────────────────────────────────────────────────────┘
```

存储：`~/.dbagent/history.db`，可设置最大保留条数 / 天数。

### 14.2 结果快照

- 每次查询结果可"📌 钉住"，保存为快照 Tab
- 快照在应用重启后保留
- 适合"先查一份对比数据，后续再回看"

---

## 15. 命令面板（Cmd+K）

### 15.1 全局命令面板

```
┌──────────────────────────────────────────────────────────┐
│  >                                                       │
├──────────────────────────────────────────────────────────┤
│  最近                                                     │
│  ▶ 新建 SQL 查询                                          │
│  ▶ 打开 users 表                                          │
│  ▶ 切换到生产 PG 连接                                     │
│  ─────                                                    │
│  推荐                                                     │
│  ▶ 导出当前结果为 CSV                                     │
│  ▶ 格式化 SQL                                             │
│  ▶ 重建 RAG 索引                                          │
└──────────────────────────────────────────────────────────┘
```

### 15.2 操作覆盖

**所有 GUI 操作必须有对应命令**，覆盖：
- 文件：新建 SQL / 打开 / 保存 / 关闭 Tab
- 编辑：撤销 / 重做 / 格式化 / 注释
- 执行：运行 / 取消 / EXPLAIN
- 视图：切换 Chat 面板 / 切换侧栏 / 切换主题
- 连接：切换连接 / 新建连接 / 重建索引
- 数据：导入 / 导出 / 截断
- 工具：MCP 列表 / Skill 列表
- AI：开启自动模式 / 切换模型

### 15.3 实现要点

- 用 [cmdk](https://cmdk.paco.me/) 库（与 Linear、Vercel 同款）
- 支持模糊搜索
- 命令分组 + 图标
- 最近使用排序
- 用户可自定义快捷键

---

## 16. 快捷键完整清单

### 16.1 全局

| 快捷键 | 功能 |
|---|---|
| Cmd+K | 命令面板 |
| Cmd+P | 快速切换 Tab / 文件 |
| Cmd+, | 设置 |
| Cmd+N | 新建 SQL 查询 |
| Cmd+Shift+N | 新建会话 |
| Cmd+L | 切换 Chat 面板 |
| Cmd+B | 切换左侧栏 |
| Cmd+J | 切换底部面板 |
| Cmd+W | 关闭 Tab |
| Cmd+Shift+T | 撤销关闭 |
| Cmd+T | 全局搜索表 |
| Cmd+/ | 切换 Agent 模式 |

### 16.2 SQL 编辑器

| 快捷键 | 功能 |
|---|---|
| Cmd+Enter | 运行全部 |
| Cmd+Shift+Enter | 运行选中 |
| Cmd+E | 运行光标处语句 |
| Cmd+Shift+E | EXPLAIN |
| Esc | 取消执行 |
| Cmd+S | 保存到文件 |
| Cmd+Shift+F | 格式化 |
| Cmd+/ | 注释/取消注释 |
| Cmd+D | 选中下一相同词 |

### 16.3 表数据

| 快捷键 | 功能 |
|---|---|
| Enter / F2 | 编辑当前单元格 |
| Esc | 取消编辑 |
| Tab / Shift+Tab | 移到下/上一单元格 |
| Cmd+S | 提交修改 |
| Cmd+Z | 撤销修改 |
| Cmd+R | 刷新数据 |
| Cmd+F | 行内搜索 |

### 16.4 自定义

设置中可重映射任意快捷键，遵循 VS Code keybinding 风格。

---

## 17. 与 AI 的融合点（汇总）

> 这是本项目的核心差异化：传统能力扎实 + AI 无缝叠加。

| 传统场景 | AI 增强 |
|---|---|
| SQL 编辑器写 SQL | 选中 + Cmd+K → AI 改写（diff 预览） |
| 表数据浏览 | 单元格右键 → "AI 解读这个 JSON" |
| EXPLAIN 看不懂 | [🤖 让 AI 解读] |
| 设计表想不出字段名 | 表设计器 [🤖 AI 建议字段] |
| 看到错误不知怎么修 | 错误旁 [🤖 AI 修复] 按钮 |
| 表数据中筛选条件难写 | 筛选器 [🤖 描述需求] → 自动生成 WHERE |
| 不知道某个表干嘛的 | 表节点 [🤖 解释这个表] |
| 写不出复杂 JOIN | Chat → 自然语言 → 生成 SQL → 一键打开为编辑器 Tab |

**关键设计**：所有 AI 入口都不打断传统流程，且 AI 输出可"接管"为传统编辑对象。

---

## 18. 性能与轻量化策略

### 18.1 启动期

- 仅加载用户上次打开的连接（非全部）
- Schema 树懒加载
- Tab 状态恢复采用占位 → 用户激活时再加载

### 18.2 内存

- 大结果集分页 + 虚拟滚动，不全量驻留
- 编辑器 Tab 关闭后立即释放 Monaco model
- 历史记录用 SQLite 而非内存

### 18.3 包体积

- Monaco 按需加载语言包（仅 SQL）
- 不打包所有数据库驱动（PG 优先，其他按需动态加载）
- 静态资源压缩（图标用 SVG sprite）
- 主题/语言文件单独 chunk

### 18.4 运行时

- DB 查询超时默认 60s，可配置
- 编辑器自动保存延迟 2s（防抖）
- 表树自动刷新限频（每 30s 最多一次）

---

## 19. 错误处理与友好提示

### 19.1 错误分级

| 级别 | 视觉 | 处理 |
|---|---|---|
| Info | 蓝色 toast | 1.5s 自动消失 |
| Success | 绿色 toast | 1.5s 自动消失 |
| Warning | 黄色 toast | 5s 自动消失 / 用户关闭 |
| Error | 红色 modal | 用户必须关闭 |
| Fatal | 全屏 error boundary | 提供重启选项 |

### 19.2 错误内容

- 中文友好（数据库英文错误自动翻译/解释）
- 提供"复制错误信息"按钮
- 严重错误提供 [🤖 让 AI 帮忙] 按钮

### 19.3 撤销机制

- 表数据编辑：本地 undo 栈（不限层）
- 表设计器：本地 undo 栈
- 已提交到数据库的写操作：仅记录在历史，不自动 ROLLBACK（除非在事务内）
- 超长事务（> 5 分钟）警告 + 询问是否继续

---

## 20. 模块化与扩展点

| 扩展点 | 接口 | 用途 |
|---|---|---|
| 数据库类型 | `IDbDriver` + `IDbDialect` | MySQL/Oracle/etc |
| 字段类型编辑器 | `ICellEditor` | 自定义类型（如 PostGIS） |
| 导入格式 | `IImportProvider` | Parquet 等 |
| 导出格式 | `IExportProvider` | 自定义格式 |
| 表节点右键菜单 | `ITableContextMenuProvider` | 插件可扩展 |
| Tab 类型 | `ITabType` | 自定义视图 |
| 命令 | `ICommand` | 注册到命令面板 |

---

## 21. MVP 范围明确

### 21.1 M1（基础可用，必做）

- ✅ 连接管理（PG）
- ✅ Schema 树（懒加载）
- ✅ SQL 编辑器（Monaco + 高亮 + 补全）
- ✅ 执行 + 结果表格（虚拟滚动 + 分页）
- ✅ 结果导出（CSV / Excel / JSON）
- ✅ 命令面板（Cmd+K）
- ✅ Tab 系统

### 21.2 M2~M3（核心丰富，必做）

- ✅ 表数据浏览（筛选/排序/列管理）
- ✅ 表数据编辑 + 事务保护
- ✅ 表设计器（字段/索引/外键）
- ✅ ER 图（mermaid 自动生成）
- ✅ EXPLAIN 可视化
- ✅ SQL 历史
- ✅ AI 改写（融合点）

### 21.3 M4~M5（高级功能，可选）

- ⏸ 视图编辑器
- ⏸ 函数 / 存储过程编辑器
- ⏸ 用户与权限管理
- ⏸ 数据导入向导

### 21.4 不进 MVP

- ❌ 拖拽式 ER 编辑
- ❌ 数据同步 / 数据传输
- ❌ 备份还原
- ❌ 计划任务调度
- ❌ 模型逆向工程（已有 schema → 实体类生成）

---

## 22. 待定与未来

- [ ] **Notebook 模式**：单 Tab 内多 cell（SQL + 文本 + 图表混排），类似 Jupyter
- [ ] **数据透视表**：结果集快速透视分析
- [ ] **Excel 双向同步**：导出后修改可导回
- [ ] **协作模式**：多人共享 SQL 文件 / 会话
- [ ] **数据血缘**：跨表跨视图的字段血缘可视化
- [ ] **慢查询监控**：连接到 pg_stat_statements 自动展示
