# BetaV0.1.1 账号标识校验补齐

## 背景

账号体系支持邮箱验证码和手机号验证码。前端会根据输入自动判断通道，但认证服务层也必须独立校验，避免未来插件、脚本或 IPC 调用绕过前端约束。

## 本次实现

- 注册时禁止同时传入邮箱和手机号。
- 邮箱验证码请求必须使用合法邮箱格式。
- 手机验证码请求必须使用合法手机号格式，支持 `+`、空格和短横线。
- 验证码登录和忘记密码重置同样校验目标与通道一致。

## 测试

- `auth-service.test.ts` 覆盖错误通道、非法邮箱、非法手机号和双标识注册拒绝。
