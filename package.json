{
  "name": "dbagent",
  "version": "0.1.0",
  "private": true,
  "description": "Embeddable AI Database Agent Runtime for natural-language SQL and database operations.",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20.11.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "pnpm build:server && node apps/server/dist/cli.js",
    "start": "node apps/server/dist/cli.js",
    "build:server": "tsc -b packages/shared packages/core-usage packages/core-llm packages/core-db packages/core-rag packages/core-skills packages/core-agent packages/core-tools packages/sdk apps/server",
    "test:server": "vitest run packages/sdk apps/server --pool=forks",
    "package:npm": "tsc -b packages/shared packages/core-usage packages/core-llm packages/core-db packages/core-rag packages/core-skills packages/core-agent packages/core-tools packages/sdk apps/server && node scripts/package-npm.mjs",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "test": "turbo test",
    "test:postgres": "node scripts/run-postgres-tests.mjs",
    "smoke": "node scripts/smoke.mjs",
    "ci": "pnpm typecheck && pnpm lint && pnpm test && pnpm smoke",
    "db:up": "docker compose -f scripts/dev-db/docker-compose.yml up -d",
    "db:down": "docker compose -f scripts/dev-db/docker-compose.yml down -v"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/node": "^22.10.2",
    "esbuild": "^0.25.12",
    "eslint": "^9.17.0",
    "prettier": "^3.4.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.2",
    "vitest": "^2.1.8"
  }
}
